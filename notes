todo's:
1. DRAMSIM2
	- Need to get the numbers for other apps as well...
	- Same parameters? 
3. altera: (resolved. example command:quartus_pow MAX4MAIAPeripheryTop --default_input_io_toggle_rate=12.5% --default_toggle_rate=12.5% . please save the new make file. test for arginout starts at 11:23)
	- see if it is possble to take the compile results from maxj
	- try to only get hdl files and synthesize using the gui (high priority)
		- try utilizing the generated reports from altera (medium)
4. CACTI:
	- how would the power / area change according to different words / size
5. generate verilog for CU
	- bottom-up compiling (high)
2. collect resources usage for these things in the excel sheet
